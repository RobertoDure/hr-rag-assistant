package ie.com.rag.entity;

/**
 * User role enumeration
 * ADMIN: Full system access
 * HR_MANAGER: Can manage candidates and job analyses
 * USER: Read-only access
 */
public enum UserRole {
    ADMIN,
    HR_MANAGER,
    USER
}
